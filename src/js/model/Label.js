export class Label {
    /**
     * 
     * @param {string} id 
     * @param {string} text 
     * @param {string} color 
     */
    constructor(id, text, color){
        this.id = id;
        this.text = text;
        this.color = color;
    }
}